import 'package:flutter/material.dart';

class InfoGeneralView extends StatelessWidget {
  const InfoGeneralView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Información general'));
  }
}
